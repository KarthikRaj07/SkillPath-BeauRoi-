USE SRS;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    job VARCHAR(100),
    skills TEXT
);


ALTER TABLE users
CHANGE COLUMN job current_job VARCHAR(100);

ALTER TABLE users
ADD COLUMN required_job VARCHAR(100);


