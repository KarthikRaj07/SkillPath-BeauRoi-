USE SRS;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    job VARCHAR(100),
    skills TEXT
);


ALTER TABLE users
ADD COLUMN password_hash VARCHAR(255) NOT NULL;
